// 遍历行
for (var i = 1; i <= 9; i++) {
    // 每行维护一个字符串，这一行的字符串都是拼接上去的
    var str = ''
    // 迭代9项
    for (var j = 1; j <= 9; j++) {

        // 拼接字符串，直把后面这句话往str空字符串里面加（a+=b=a+b）
        str += `${i}乘${j}得${i * j}\t`

        //每行输出一条字符串，但是这个字符串中是9个算式
        console.log(str)
    }
}